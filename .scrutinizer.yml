filter:
    paths:
        - Api/src/*

build:
    project_setup:
        before:
            - 'cd Api'
            - 'composer install'
    tests:
        override:
            -
                command: './bin/phpspec run -f progress -c ./phpspec-ci.yml'
                coverage:
                    file: 'coverage.clover'
                    format: 'php-clover'
